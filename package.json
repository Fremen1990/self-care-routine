{
  "name": "self-care-routine",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "web:dev": "bun --cwd apps/web run dev",
    "web:build": "bun --cwd apps/web run build",
    "web:start": "bun --cwd apps/web run start",
    "mobile:start": "bun --cwd apps/mobile run start",
    "mobile:android": "bun --cwd apps/mobile run android",
    "mobile:ios": "bun --cwd apps/mobile run ios",
    "mobile:web": "bun --cwd apps/mobile run web",
    "eas": "bun --cwd apps/mobile run eas"
  }
}
